# Marketing site images

Every static image rendered by the marketing site lives here.

## Current inventory

| File | Used by | Source |
|---|---|---|
| `screenshots/{overview,usage,sessions,projects,models,settings}-{en,zh}-{dark,light}.png` | Homepage hero/frame, feature cards, and `/features/` gallery | Real product screenshots from `pnpm screenshots`; rendered by locale and current resolved theme |
| `feature-cli.webp` | "CLI report" feature card | AI-generated, per prompt below |
| `feature-mcp.webp` | "MCP server" feature card | AI-generated |
| `feature-heatmap.webp` | "Activity heatmap" feature card | AI-generated |
| `feature-privacy.webp` | "100% local" feature card | AI-generated |
| `feature-i18n.webp` | "Bilingual + themeable" feature card | AI-generated |
| `og-default.png` | Default `og:image` (homepage, features, mcp, privacy) | HTML/CSS render using `favicon.svg`, 1200×630 |
| `og-cli.png` | `og:image` for `/cli/` | HTML/CSS render using `favicon.svg`, 1200×630 |

### Legacy SVG placeholders (kept, not used by production pages)

`hero-dashboard.svg`, `feature-{cli,mcp,privacy,i18n,dashboard}.svg`,
`og-{default,cli}.svg` — generated by [`../../scripts/gen-placeholders.mjs`](../../scripts/gen-placeholders.mjs)
during early scaffolding. None of the live `.astro` pages reference them
anymore. They stay in tree because:

1. The generator script is useful if you need fresh brand-matched
   placeholders while iterating on layout.
2. They cost ~50 KB total and ship in `dist/_astro` only if referenced,
   so leaving them on disk is free.

If you ever clean house, delete the SVGs **and** drop the
`site:gen:placeholders` script from the root `package.json`.

## Refresh workflow

### When the dashboard UI changes

The `screenshots/*.png` files come from the main repo's
[`scripts/screenshots.mjs`](../../../scripts/screenshots.mjs). To
regenerate:

```bash
cd ../..                      # back to repo root
pnpm screenshots              # writes locale × theme variants to docs/screenshots/
cp docs/screenshots/*.png \
   site/public/images/screenshots/
pnpm site:build              # rebuild marketing site
```

### When a feature card or OG image changes

1. Re-generate feature cards using the prompt for that filename (catalogue below).
2. Re-render OG cards from the current `site/public/favicon.svg` so the social
   preview stays aligned with the product logo.
3. Export as **WebP** at 16:10 (feature cards) or **PNG** at 1200×630 (OG).
4. Drop the file into `public/images/` overwriting the existing one.
5. Rebuild with `pnpm site:build`.

If you rename a file, grep the `site/src/pages/` and `site/src/layouts/`
trees for the old name and update every reference.

## Prompt catalogue

All prompts target the same dark-indigo aesthetic: near-black background,
Indigo-400 (`#818CF8`) accents, soft glows, premium dev-tool marketing
illustration (Vercel / Linear / Stripe vibe). **No readable text, no
real logos.**

### `feature-cli.webp` — 1600 × 1000 (16:10)

> Stylized macOS terminal window centered on near-black background
> `#0A0A0A`, indigo (#818CF8) cursor and accent, white and muted-gray
> monospace text characters in abstract not-quite-readable form, a small
> ASCII-art horizontal bar chart inside the terminal with indigo bars,
> subtle indigo glow under the window, flat-vector aesthetic, minimal
> shadows, premium dev-tool marketing illustration.

### `feature-mcp.webp` — 1600 × 1000

> Abstract network diagram: three glowing indigo (#818CF8) nodes
> connected by thin curved lines on a near-black canvas, central node
> larger with a subtle pulse glow, two outer nodes labelled with abstract
> glyphs (no readable text), geometric and clean, technical-illustration
> style reminiscent of Stripe and Linear marketing art, soft volumetric
> lighting.

### `feature-heatmap.webp` — 1600 × 1000

> 7×24 abstract day-of-week × hour-of-day heatmap grid floating on a
> near-black background `#0A0A0A`, cells filled with varying intensities
> of indigo (#818CF8), a few darker (low activity) and a few near-pure
> indigo (peak hours), thin grid lines, subtle indigo glow underneath
> the grid, isometric perspective with mild depth, no axis labels, no
> readable text, premium data-visualization marketing aesthetic.

### `feature-privacy.webp` — 1600 × 1000

> Translucent indigo (#818CF8) shield icon overlaid on a stylized solid-
> state drive silhouette, near-black background, soft indigo glow
> underneath, geometric isometric design, no network arrows leaving the
> drive, no telemetry beams, clean minimalist tech-illustration aesthetic,
> premium "privacy by design" visual.

### `feature-i18n.webp` — 1600 × 1000

> Two stacked browser-window mockups on near-black background — top
> window shows abstract Latin character blocks in indigo (#818CF8),
> bottom window shows abstract CJK character blocks in the same indigo,
> the two windows linked by a thin indigo curved arc, minimalist
> geometric style, soft glow, no readable text, premium
> internationalisation aesthetic.

### `og-default.png` — 1200 × 630

Rendered from HTML/CSS with `site/public/favicon.svg` as the brand mark:
near-black background, indigo glow/grid, ccgauge wordmark, local-first
badge, and a dashboard-style chart panel on the right.

### `og-cli.png` — 1200 × 630

Rendered from HTML/CSS with `site/public/favicon.svg` as the brand mark:
near-black background, indigo glow/grid, ccgauge wordmark, and a terminal
panel featuring `$ ccgauge report` plus abstract report rows.

## Aspect-ratio reference

| Use | Aspect | Export size |
|---|---|---|
| Feature card (`feature-*.webp`) | 16:10 | 1600 × 1000 |
| OG / Twitter card (`og-*.png`) | 1.91:1 | 1200 × 630 |
| Dashboard screenshot (`screenshots/*.png`) | matches source | 2880 × 1800 @ 2× retina |

## Budget

Keep `public/images/` under ~8 MB total. Current footprint:
~7.3 MB (the locale × theme screenshot matrix dominates at ~6.2 MB; feature
WebPs ~250 KB; OG PNGs ~800 KB). Real-product screenshots are the heaviest
items but also the most credible — don't try to slim them by re-encoding past
visual fidelity.
